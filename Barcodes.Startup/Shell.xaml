<Window x:Class="Barcodes.Startup.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:Barcodes.Core;assembly=Barcodes.Core"
        xmlns:local="clr-namespace:Barcodes.Startup"
        mc:Ignorable="d"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="Barcodes" 
        Height="{StaticResource ShellHeight}" 
        Width="{StaticResource ShellWidth}"
        MinHeight="{StaticResource ShellHeight}" 
        MinWidth="{StaticResource ShellWidth}"
        Icon="icon.ico">
    <Grid>
        <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.Core}" />
    </Grid>
</Window>
