<UserControl x:Class="Barcodes.Core.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:Barcodes.Core.UI.Controls;assembly=Barcodes.Core.UI"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800"
             prism:ViewModelLocator.AutoWireViewModel="True" Loaded="UserControl_Loaded">
    <DockPanel LastChildFill="True">
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding BarcodesCount, StringFormat='Barcodes count: {0}'}" />
            </StatusBarItem>
            <Separator Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <Button Content="?" />
            </StatusBarItem>
        </StatusBar>
        <StackPanel>
            <Image Width="200"
                   Height="200"
                   Margin="0 10 0 0"
                   Source="{Binding RandomBarcode}" />
            <Label Content="Title" />
            <TextBox Width="150"
                     Text="{Binding Title}" />
            <Label Content="Data" />
            <TextBox Width="150"
                     Text="{Binding Data}" />
            <ComboBox Width="150"
                      ItemsSource="{Binding BarcodeTypes}"
                      SelectedItem="{Binding SelectedBarcodeType}"
                      DisplayMemberPath="TypeTitle" />
            <Button Content="Generate"
                    Command="{Binding GenerateBarcodeCommand}" />
            <Label Content="Selected action:" />
            <ComboBox Width="150"
                      ItemsSource="{Binding SelectedActions}"
                      SelectedItem="{Binding CurrentSelectedAction}"
                      DisplayMemberPath="Title" />
            <Button Content="Execute"
                    Command="{Binding ExecuteSelectedActionCommand}" />
            <ListView Height="300"
                      SelectionMode="Single"
                      ItemsSource="{Binding Barcodes}"
                      SelectedItem="{Binding SelectedBarcode}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <prism:InvokeCommandAction Command="{Binding OpenInNewWindowCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Width="{Binding (FrameworkElement.ActualWidth), RelativeSource={RelativeSource AncestorType=ScrollContentPresenter}}"
                                   ItemWidth="{Binding (ListView.View).ItemWidth, RelativeSource={RelativeSource AncestorType=ListView}}"
                                   MinWidth="{Binding ItemWidth, RelativeSource={RelativeSource Self}}"
                                   ItemHeight="{Binding (ListView.View).ItemHeight, RelativeSource={RelativeSource AncestorType=ListView}}" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <controls:BarcodeView />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </DockPanel>
</UserControl>
